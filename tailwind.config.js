const colors = require('tailwindcss/colors')

module.exports = {
    theme: {
        colors: {
            crypto:{
                light: '#100f0f',
                button: '#efefef',
            }
        }
    }
}